# Server
PORT=8080
ENV=development

# MongoDB (overridden to service name inside docker-compose)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=shipping_system

# Redis (overridden to service name inside docker-compose)
REDIS_ADDR=localhost:6379
REDIS_DB=0

# JWT — change this in production
JWT_SECRET=change-me-in-production

# Logging
LOG_LEVEL=info
