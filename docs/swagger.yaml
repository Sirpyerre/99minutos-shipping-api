basePath: /
definitions:
  github_com_99minutos_shipping-system_internal_core_domain.User:
    properties:
      client_id:
        type: string
      created_at:
        type: string
      email:
        type: string
      id:
        type: string
      role:
        type: string
      updated_at:
        type: string
      username:
        type: string
    type: object
  internal_api_handler.authResponse:
    properties:
      token:
        type: string
      user:
        $ref: '#/definitions/github_com_99minutos_shipping-system_internal_core_domain.User'
    type: object
  internal_api_handler.dependencyStatus:
    properties:
      error:
        type: string
      status:
        type: string
    type: object
  internal_api_handler.loginRequest:
    properties:
      email:
        type: string
      password:
        type: string
    type: object
  internal_api_handler.readinessResponse:
    properties:
      dependencies:
        additionalProperties:
          $ref: '#/definitions/internal_api_handler.dependencyStatus'
        type: object
      status:
        type: string
    type: object
  internal_api_handler.registerRequest:
    properties:
      client_id:
        type: string
      email:
        type: string
      password:
        type: string
      role:
        type: string
      username:
        type: string
    type: object
host: localhost:3000
info:
  contact:
    name: 99minutos Engineering
    url: https://99minutos.com
  description: Event-driven shipment tracking system for 99minutos. Processes 10,000+
    events/second with guaranteed per-shipment ordering.
  license:
    name: MIT
  termsOfService: http://swagger.io/terms/
  title: 99minutos Shipping Tracking API
  version: "1.0"
paths:
  /auth/login:
    post:
      consumes:
      - application/json
      parameters:
      - description: Login credentials
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/internal_api_handler.loginRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/internal_api_handler.authResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Login
      tags:
      - auth
  /auth/register:
    post:
      consumes:
      - application/json
      parameters:
      - description: User registration details
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/internal_api_handler.registerRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/internal_api_handler.authResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "409":
          description: Conflict
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Register a new user
      tags:
      - auth
  /health:
    get:
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Liveness probe
      tags:
      - health
  /health/ready:
    get:
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/internal_api_handler.readinessResponse'
        "503":
          description: Service Unavailable
          schema:
            $ref: '#/definitions/internal_api_handler.readinessResponse'
      summary: Readiness probe
      tags:
      - health
securityDefinitions:
  BearerAuth:
    description: Type "Bearer" followed by a space and JWT token.
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
